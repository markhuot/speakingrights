{%- macro fieldName(name) -%}
    {% set langcode = getCookie('langcode')|default('en') %}
    {% if langcode == 'fr' %}
        {{ name }}Fr
    {% else %}
        {{ name }}
    {% endif %}
{%- endmacro -%}